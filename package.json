{
  "name": "aws-devicefarm-mobile-device-testing",
  "version": "1.0.0",
  "description": "Carries out an Automated Test on AWS Device Farm",
  "main": "index.js",
  "scripts": {
    "lint": "eslint **.js",
    "package": "ncc build index.js -o dist && ncc build cleanup.js -o dist/cleanup",
    "test": "jest --coverage --verbose"
  },
  "keywords": [
    "AWS",
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "AWS",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@aws-sdk/client-device-farm": "^3.382.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "aws-sdk-client-mock": "^3.0.0",
    "aws-sdk-client-mock-jest": "^3.0.0",
    "eslint": "^8.47.0",
    "jest": "^29.6.2",
    "mock-fs": "^5.2.0"
  }
}
